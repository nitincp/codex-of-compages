# M1 — Layer 0 PoC: Framework Builders
#
# What this milestone proves: all 9 framework builders produce correctly structured
# output and can be composed via ComposedPrompt. Nothing else can be built until
# this is proven.
#
# GNN contribution: FrameworkLayer nodes — one per builder.
# Node feature: build_output (the actual prompt structure this framework produces).
# This is the foundational feature layer all later cross-schema edges reference.
